apiVersion: 1

providers:
  - name: "AgentBazaar"
    orgId: 1
    folder: "AgentBazaar"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
